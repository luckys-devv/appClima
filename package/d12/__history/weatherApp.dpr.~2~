program weatherApp;

uses
  System.StartUpCopy,
  FMX.Forms,
  FLogin in '..\..\source\FLogin.pas' {Form1},
  FMain in '..\..\source\FMain.pas' {Form2};

{$R *.res}

begin
  Application.Initialize;
  Application.CreateForm(TForm1, Form1);
  Application.CreateForm(TForm2, Form2);
  Application.Run;
end.
